#include <stdio.h>

/*
Este es simplemente hacer la media aritmética (ya que tienen la misma velocidad)
(70 + 150)/2
*/

int main(void){
  printf("Kilómetro en que se encontrarán: %d\n", (70 + 150) >> 1);

  return 0;
}
